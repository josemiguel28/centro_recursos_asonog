# Estructura de la base de datos
- `crgc_db_tablas` contiene todas las tablas por separado
- `crgc_self-contained_db` contiene un solo script de toda la base de datos

Nota: antes de ejecutar cualquiera de los script, antes ejecutar 
`CREATE DATABASE centro_recursos_asonog;`